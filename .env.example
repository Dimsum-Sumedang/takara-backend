# System Settings
PROJECT_NAME="Takara"
VERSION="1.0.0"

# AI & Voice Providers
GROQ_API_KEY=gsk_

# Database
DATABASE_URL=postgresql://

# Auth
JWT_SECRET=<generate-random-32-char-min>

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Takara <no-reply@takara.com>"